name: Publish npm packages

on:
  push:
    tags:
      - 'sdk/typescript/v*'
      - 'apps/cli/typescript/v*'

jobs:
  publish-core:
    if: startsWith(github.ref, 'refs/tags/sdk/typescript/')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # required for OIDC trusted publishing
    environment: npm
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm (trusted publishing requires npm >= 11.5.1)
        run: npm install -g npm@latest

      - name: Install and build
        working-directory: sdk/typescript
        run: npm ci && npm run build

      - name: Run tests
        working-directory: sdk/typescript
        run: npm test

      - name: Publish @pyhall/core
        working-directory: sdk/typescript
        run: npm publish --access public

  publish-cli:
    if: startsWith(github.ref, 'refs/tags/apps/cli/typescript/')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # required for OIDC trusted publishing
    environment: npm
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm (trusted publishing requires npm >= 11.5.1)
        run: npm install -g npm@latest

      - name: Install SDK dep
        working-directory: sdk/typescript
        run: npm ci && npm run build

      - name: Install CLI deps
        working-directory: apps/cli/typescript
        run: npm ci && npm run build

      - name: Run tests
        working-directory: apps/cli/typescript
        run: npm test

      - name: Publish @pyhall/cli
        working-directory: apps/cli/typescript
        run: npm publish --access public
